class Frame:
    """
    Base class for reference frames.

    Frames are empty classes used purely as a "marker" type so that the type-checker can reason
    about the frame in which a value was measured.
    """

    pass
