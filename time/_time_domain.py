class TimeDomain:
    """
    Base class for reference time domains.

    TimeDomains are empty classes used purely as a "marker" type so that the type-checker can reason
    about timestamps.
    """

    pass
